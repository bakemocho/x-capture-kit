# Bookmarklet

Generate bookmarklet code from the shared core script:

```bash
node ../scripts/build-bookmarklet.js
```

Then create a browser bookmark and paste the generated `javascript:` URL as its address.
